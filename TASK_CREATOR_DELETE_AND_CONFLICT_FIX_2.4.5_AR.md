# إصلاح حذف منشئ المهمة وتعارض الحفظ 2.4.5

## القواعد بعد التحديث

- يستطيع الموظف أو المدير حذف المهمة غير المكتملة عندما يكون هو منشئها.
- لا يسمح الحذف لمجرد أن المستخدم هو المكلف بالمهمة أو المدير المباشر للمكلف.
- يستطيع مدير النظام حذف جميع المهام، وتبقى المهام المكتملة محصورة به.

## معالجة خطأ بدء المهمة ونسبة الإنجاز

- يعيد النظام قراءة أحدث إصدار من المهمة عند حدوث تعارض لحظي.
- يعيد المحاولة مرة واحدة فقط، ويقتصر ذلك على خطأ تعارض الإصدار.
- لا يعيد تنفيذ انتقال الحالة إذا تغيّرت حالة المهمة فعليًا، ولا يكتب نسخة قديمة فوق نسخة أحدث.

## خطوات التطبيق

1. ارفع ملفات الحزمة إلى جذر المستودع مع المحافظة على المسارات.
2. نفّذ `supabase/migrations/20260915_creator_delete_and_task_update_resilience.sql` مرة واحدة في Supabase SQL Editor. هذا الملف يحل محل قاعدة الحذف السابقة بأمان.
3. انتظر نجاح `ATWAR Quality Gate` و`pages-build-deployment`.
4. اختبر بحساب عباس بخش: بدء مهمة حالتها `قيد الانتظار`، ثم تغيير نسبة الإنجاز لمهمة `قيد التنفيذ`.
5. اختبر حذف مهمة أنشأها عباس بنفسه، ثم تحقق من اختفاء الحذف في مهمة أنشأها مستخدم آخر.
