# إصلاح صلاحية حذف المهام 2.4.4

## القاعدة المعتمدة

- مدير النظام: يستطيع حذف المهمة، بما فيها المهمة المكتملة.
- المدير: يستطيع حذف المهمة غير المكتملة فقط عندما يكون هو منشئها.
- المدير المباشر للمكلف: لا يستطيع حذف المهمة لمجرد أنها ضمن فريقه.
- الموظف: لا يستطيع حذف المهام.

## خطوات التطبيق

1. ارفع ملفات الحزمة إلى جذر المستودع مع المحافظة على المسارات.
2. نفّذ ملف `supabase/migrations/20260915_restrict_manager_task_deletion.sql` مرة واحدة من Supabase SQL Editor.
3. انتظر نجاح `ATWAR Quality Gate` و`pages-build-deployment`.
4. اختبر بحساب مدير على مهمتين: مهمة أنشأها المدير ومهمة أنشأها شخص آخر. يجب أن يظهر الحذف في الأولى غير المكتملة فقط.

الحماية مطبقة في الواجهة، وعند لحظة التنفيذ، وداخل قاعدة البيانات للحذف الناعم والنهائي.
