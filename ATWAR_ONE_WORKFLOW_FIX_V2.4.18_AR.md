# إصلاح مسار الأوصاف والتفويض V2.4.18

## ما تم إصلاحه

- يسمح بإرسال وصف الرئيس التنفيذي عند تعيين مراجع صريح، حتى لو لم يكن له مدير مباشر.
- يحوّل «إنهاء مراجعة المدير» إلى حالة فعلية `MANAGER_APPROVED` تظهر لمدير النظام باسم «موافقة المدير مكتملة» مع إشعار داخل النظام.
- يضيف قسم «ربط الوصف بالموظف» داخل صفحة الوصف لمدير النظام، مع إشعار عند الربط وإشعار آخر عند الاعتماد والنشر.
- يوجّه إشعار الوصف إلى صفحة الملف الوظيفي مباشرة.
- يضيف زرًا ظاهرًا باسم «تفويض المهمة» وأيقونة بجانب المسؤول الحالي للمدير الذي أُسندت إليه المهمة.

## خطوات النشر

1. ارفع ملفات الحزمة إلى مستودع الموقع وانشرها.
2. في Supabase > SQL Editor شغّل الملف:
   - `supabase/migrations/20260920_job_review_handoff_and_assignment_notifications.sql`
3. حدّث الصفحة تحديثًا قسريًا بـ `Ctrl + F5`.

## اختبار القبول

1. افتح وصف الرئيس التنفيذي كمدير نظام، عيّن الرئيس التنفيذي أو موظفًا آخر في «المراجع المعتمد»، احفظ، ثم اضغط «إرسال للمدير».
2. من حساب المراجع اضغط «إنهاء مراجعة المدير»؛ يجب أن تتغير الحالة مباشرة إلى «موافقة المدير مكتملة» ويصل إشعار لمدير النظام.
3. من صفحة الوصف اربطه بموظف. قبل النشر يظهر تنبيه بأنه سيظهر بعد النشر، وبعد النشر يصل إشعار ويمكن فتحه من الملف الوظيفي.
4. افتح مهمة مسندة إلى مدير ولديه موظفون تابعون؛ يجب أن يظهر زر «تفويض المهمة» بجانب اسم المسؤول.

## الاطلاع التنفيذي

يُمنح من: **إدارة النظام > الحسابات > اطلاع تنفيذي**. الصلاحية المستخدمة هي `tasks.read_all`؛ تعرض كل المهام للقراءة فقط، وتمنع الرئيس التنفيذي من تعديل أو حذف أو اعتماد مهام خارج نطاقه الإداري، وتسجل فتح نطاق جميع المهام في `executive_task_access_log`.
