# إصلاح عنوان Supabase Storage V2.4.16

## سبب المشكلة

كان سكربت النسخ يزيل الشرطة الأخيرة فقط من `SUPABASE_URL`. إذا احتوت القيمة على مسار مثل `/rest/v1`، كان مسار Storage يُضاف بعده وتستقبل PostgREST عنوانًا غير صالح برسالة `PGRST125`.

## المعالجة

- تحليل `SUPABASE_URL` كعنوان URL كامل.
- استخدام `origin` فقط تلقائيًا قبل إضافة `/storage/v1`.
- رفض القيم غير الصالحة برسالة واضحة.
- إضافة اختبار يمنع عودة تركيب المسار القديم.

لا يحتاج هذا الإصلاح إلى تغيير الأسرار مرة أخرى.
