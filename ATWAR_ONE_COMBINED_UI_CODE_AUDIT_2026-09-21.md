# المراجعة الموحدة للتصميم والكود — ATWAR ONE

**التاريخ:** 21 سبتمبر 2026  
**الهدف:** إعداد نطاق تعديل واحد يرفع النظام بصريًا وتقنيًا، ويمنع العودة إلى ترقيعات متفرقة.  
**الحالة:** مراجعة فقط؛ لم يتم تعديل النظام أو قاعدة البيانات.

## القرار التنفيذي

يمكن تنفيذ التحديث في **إصدار إنتاج واحد**، لكن لا ينبغي كتابته كتعديل ضخم غير مقسّم. الأسلوب الآمن هو:

1. فرع تطوير واحد.
2. مراحل داخلية صغيرة قابلة للاختبار.
3. بوابة جودة بعد كل مرحلة.
4. اختبار أدوار الموظف والمدير ومدير النظام.
5. حزمة نشر إنتاج واحدة بعد نجاح جميع المراحل.

بهذا نحقق طلب “التعديل مرة واحدة” للمستخدمين، دون المخاطرة بتغيير النظام كله دفعة واحدة بلا نقاط رجوع.

## ملخص الوضع الحالي

- 36 صفحة HTML.
- 16 ملف JavaScript و29 ملف MJS تشمل التطبيق والاختبارات والنسخ الاحتياطي.
- 18 Migration لقاعدة البيانات.
- 5 ملفات CSS، مع وجود تصميمات كثيرة داخل الصفحات نفسها.
- 24 اختبارًا في بوابة الجودة، وجميعها ناجحة حاليًا.
- أكبر ملف هو `assets/js/tasks-page.js` ويبلغ 3,014 سطرًا.
- 60 استعلامًا تستخدم `select('*')`.
- 10 نسخ متكررة من دالة `authProfile`.
- 94 موضعًا يبني الواجهة عبر `innerHTML`.
- 16 استخدامًا لـ`alert`، و8 لـ`confirm`، و13 لـ`prompt`.
- 134 تنسيقًا مباشرًا داخل HTML.
- 4 إصدارات مختلفة على الأقل من `shell-components.js` مستخدمة بين الصفحات.

## نقاط القوة البرمجية

- جميع اختبارات الجودة الحالية نجحت.
- لا توجد مفاتيح `service_role` أو مفاتيح Resend مكشوفة مباشرة في المتصفح.
- وظائف Edge تستخدم الأسرار من متغيرات البيئة.
- العمليات الحساسة للمهام تستخدم RPC محمية بدل تحديثات مباشرة في عدد من المسارات المهمة.
- يوجد Optimistic Concurrency وتحديث جزئي للمهام بدل إرسال السجل كاملًا.
- يوجد فصل مقبول بين المرفقات ونواة المهام والواجهة.
- يوجد RLS وسياسات صلاحية واستخدام واضح لـ`auth.uid()` في أغلب الهجرات.
- مسارات الحذف والتفويض وتغيير التواريخ والإقرار الوظيفي مغطاة باختبارات تعاقدية.
- النسخ الاحتياطي الأسبوعي والتحقق منه مشمولان ببوابة الجودة.

## الملاحظات الحرجة قبل التعديل

### 1. ملف المهام أصبح Monolith

`tasks-page.js` يحتوي العرض، الأحداث، النوافذ، الصلاحيات، الإشعارات، البحث، الفلاتر، الحفظ والتعامل مع طبقة التوافق في أكثر من 3,000 سطر.

**المخاطر:**

- تعديل التصميم قد يكسر السلوك.
- صعوبة معرفة مصدر الخطأ.
- تكرار event handlers والحالات العامة.
- بطء المراجعة والاختبار.

**المطلوب:** تقسيمه إلى وحدات دون تغيير العقود الحالية:

- `task-store.js`
- `task-permissions.js`
- `task-actions.js`
- `task-details.js`
- `task-list-view.js`
- `task-kanban-view.js`
- `task-filters.js`
- `task-notifications.js`
- `task-save-state.js`

### 2. طبقة Firebase Compatibility ما زالت قلب المهام

رغم أن البيانات انتقلت إلى Supabase، ما زال `tasks-page.js` يستورد واجهة بأسماء Firebase من `supabase-firebase-compat.js`.

**المشكلة:** هذا يبقي نموذجًا ذهنيًا قديمًا ويصعب التطوير، لكنه حاليًا يحمل منطقًا حساسًا مثل التزامن وإعادة المحاولة.

**القرار:** لا تُحذف الطبقة مباشرة. أولًا تثبيت اختبارات سلوك حقيقية، ثم استبدالها تدريجيًا بمستودع Supabase أصلي مع الحفاظ على نفس النتائج.

### 3. تكرار التحقق من الجلسة والصلاحيات

دالة `authProfile` مكررة في 10 صفحات، وبعض الصفحات تتحقق من الدور في المتصفح ثم تعتمد على RLS في الخادم.

**المطلوب:** خدمة جلسة واحدة:

- تحميل المستخدم والملف مرة واحدة.
- التحقق من النشاط والدور.
- مزامنة الهوية في Shell.
- توفير `requireRole` و`requirePermission`.
- رسالة خطأ موحدة بدل إعادة توجيه صامتة دائمًا.

### 4. الاستعلامات واسعة وتتم تصفيتها في المتصفح

توجد 60 حالة `select('*')`. الصفحة الرئيسية والبحث وبعض الصفحات تجلب عددًا كبيرًا من المهام ثم تحسب النتائج محليًا.

**الأثر:** يزداد زمن التحميل واستهلاك الشبكة كلما زادت المهام.

**المطلوب:**

- تحديد الأعمدة المطلوبة.
- استخدام Views/RPC للإحصاءات المجمعة عند الحاجة.
- Pagination للقوائم الكبيرة.
- فلاتر الخادم قبل نقل البيانات.
- عدم تحميل 500 مهمة لمساحة العمل لمجرد ربط متابعة.

### 5. صفحات وملفات خاصة مرتبطة بأشخاص

تمت إزالة المسار الشخصي المعتمد على `firebase_uid` وحذف الصفحة المستقلة `profile/financial-manager.html` بتاريخ 2026-09-23.

**الحالة:** تمت المعالجة؛ لم يعد هناك توجيه أو صفحة إنتاج مرتبطة بشخص محدد.

**المطلوب:** تحويل المحتوى إلى نموذج بيانات عام داخل مكتبة الوظائف والملف التعريفي، ثم حذف الاستثناء البرمجي بعد التحقق من تكافؤ العرض.

### 6. بناء الواجهات عبر innerHTML بكثافة

يوجد 94 موضعًا يستخدم `innerHTML`. أغلب المسارات تستخدم دوال Escape، وهذا جيد، لكن الاعتماد الكبير عليها يزيد احتمال نسيان التعقيم في تعديل مستقبلي.

**المطلوب:**

- مكونات Render مشتركة.
- استخدام `textContent` للبيانات النصية متى أمكن.
- حصر HTML الديناميكي في Helpers موثوقة.
- اختبار XSS للحقول النصية والرابطية.

### 7. رسائل المتصفح القديمة

استخدام `alert/confirm/prompt` يظهر خصوصًا في الدورية ومكتبة الوظائف والنماذج.

**المطلوب:** Dialog وToast موحدان، مع حالات loading/success/error وإتاحة لوحة المفاتيح.

### 8. الاعتماد على CDN غير مثبت

عدة صفحات تستخدم `https://unpkg.com/lucide@latest`، كما تحمل الخط من Google Fonts مباشرة.

**المخاطر:** تغير غير متوقع في المكتبة، تأخر التحميل، أو توقف الواجهة عند تعذر CDN.

**المطلوب:** تثبيت نسخة Lucide محددة أو استضافتها محليًا، واستضافة ملفات Tajawal محليًا إن أمكن، مع fallback مناسب.

### 9. لا يوجد Build/Package Workflow واضح للواجهة

المشروع Static HTML/JS ولا يوجد `package.json` مركزي أو bundling/linting تقليدي. هذا بسيط في النشر لكنه يسمح بتكرار الكود واختلاف الإصدارات.

**المطلوب:** ليس من الضروري تحويل النظام إلى React. يمكن البقاء على HTML/ES Modules مع:

- ESLint/Prettier.
- Import maps أو وحدات ES واضحة.
- Manifest إصدار واحد.
- Script يبني حزمة نشر نظيفة ويستبعد التقارير وZIP القديمة.

### 10. جذر النشر يحتوي أرشيفات وتقارير قديمة

يوجد عدد كبير من ZIP وREADME وتقارير مرحلية في الجذر. إذا كان الجذر نفسه يُرفع للاستضافة فقد تصبح ملفات لا يحتاجها المستخدم متاحة للعامة.

**المطلوب:** مجلد `dist` نظيف يحتوي فقط ملفات التشغيل، ونقل الوثائق والحزم إلى مجلد داخلي غير منشور أو Releases.

## مراجعة Supabase والأمان

### النتائج الحالية

- **Leaked Password Protection معطلة**: تنبيه أمني فعلي ويحتاج تفعيلًا من Auth متى كانت الخطة تدعمه.
- **17 دالة SECURITY DEFINER قابلة للاستدعاء من authenticated**: ليست 17 ثغرة مؤكدة؛ عدد منها مصمم عمدًا لتنفيذ عملية محمية مثل الحذف أو التفويض أو الإقرار. يجب تدقيق كل دالة للتأكد من:
  - التحقق من `auth.uid()` داخلها.
  - التحقق من الدور أو الملكية.
  - `search_path` ثابت وآمن.
  - إلغاء EXECUTE عن `PUBLIC` ومنحه فقط للدور المقصود.
  - عدم إمكانية تمرير معرف سجل يخص مستخدمًا آخر.
- 8 جداول في schema `migration` لديها RLS بلا Policies. هذه غالبًا جداول ترحيل داخلية وليست ضمن Data API. القرار الأنسب هو التأكد أن schema غير مكشوف، ثم إبقاؤها داخلية أو أرشفتها، وليس إنشاء Policies عامة لها.
- 8 مفاتيح أجنبية بلا فهارس تغطية، معظمها في مكتبة الوظائف والنماذج. يفضل إضافتها بعد التحقق من الاستعلامات الفعلية.
- 43 فهرسًا غير مستخدم. لا يُنصح بحذفها الآن؛ النظام حديث ومعدل الاستخدام قد لا يكفي لإثبات عدم الحاجة.

### نقاط تحتاج فحصًا خاصًا

- صلاحية `run_recurring_tasks_safe()`؛ يجب ألا يستطيع كل مستخدم مسجل تشغيل القوالب إن لم يكن مديرًا أو Admin.
- صلاحيات `admin_set_executive_task_access` و`admin_update_profile_details`.
- صلاحية الوصول إلى جميع المهام في الاطلاع التنفيذي يجب أن تكون من قاعدة البيانات، لا من إخفاء الرابط فقط.
- وظائف Edge الخاصة بإدارة المستخدمين والبريد يجب أن تتحقق من JWT والدور في الخادم.
- تغيير كلمة المرور والتحكم بالمستخدمين يحتاجان Audit Log واضحًا.

## الاختبارات

### الوضع الجيد

كل اختبارات بوابة الجودة الـ24 نجحت، وتشمل العقود الأساسية للمهام، الحذف، الصلاحيات، التزامن، الوظائف، النسخ الاحتياطي والثيم.

### الفجوة

جزء كبير من الاختبارات يقرأ الملفات ويبحث عن نصوص أو Patterns. هذا يحمي من حذف عقد مهم، لكنه لا يثبت أن المستخدم يستطيع إكمال الرحلة فعلًا.

**المطلوب إضافته قبل الإصدار الموحد:**

1. تسجيل دخول لكل دور.
2. إنشاء مهمة وحفظها وظهور رسالة النجاح.
3. تقدم الموظف وإرسال المهمة للاعتماد.
4. اعتماد المدير وظهور النتيجة في المكتملة.
5. تفويض مهمة لموظف تابع.
6. حذف المنشئ لمهمته وفق القيود.
7. إنشاء قالب دوري وتشغيله مرة واحدة دون تكرار.
8. فتح الاطلاع التنفيذي بالقائمة والكانبان.
9. ربط وصف وظيفي ونشره وظهوره للموظف وإقراره.
10. حفظ بيانات مستخدم وظهور تأكيد واضح.
11. اختبارات Desktop/Tablet/Mobile وRTL.
12. اختبار الوصول بلوحة المفاتيح والتباين.

## نطاق التعديل الموحد المقترح

### الحزمة A — الأساس المشترك

- Design Tokens نهائية.
- تنظيف `shared.css`.
- توحيد Shell وإصدارات assets.
- Session/Authorization Service موحدة.
- Toast/Dialog/Save State/Empty State/Loading State مشتركة.
- استضافة dependencies المثبتة محليًا.

### الحزمة B — نواة المهام

- تقسيم `tasks-page.js` إلى وحدات.
- الحفاظ على جميع قواعد الصلاحية الحالية.
- توحيد List/Kanban/Filters.
- تحسين استعلامات المهام وPagination.
- الانتقال التدريجي من Firebase compatibility إلى Supabase repository أصلي.

### الحزمة C — صفحات المهام الأربع

- النشطة، المكتملة، الدورية، والاطلاع التنفيذي بنفس القالب.
- إزالة التمرير الداخلي والشطب من المكتملة.
- فصل إنشاء القالب الدوري عن قائمة القوالب.
- تصغير نظرة الفريق.
- تأكيد الحفظ والتنفيذ والحذف داخل الصفحة.

### الحزمة D — الملف التعريفي والوظائف

- الملف التعريفي وجهة موحدة.
- دمج الوصف والمهام والصلاحيات وKPI.
- إزالة الاستثناء الخاص بالمدير المالي بعد نقل بياناته للنموذج العام.
- توحيد مكتبة الوظائف والمراجعة والإقرار.

### الحزمة E — الإدارة وبقية النظام

- إعادة تنظيم إدارة المستخدمين.
- توحيد النماذج والإعلانات والأدوار والصيانة والفريق ومساحة العمل.
- إزالة `alert/prompt`.
- حفظ واضح وتدقيق لكل عملية حساسة.

### الحزمة F — قاعدة البيانات والأمان والأداء

- تدقيق 17 RPC من نوع SECURITY DEFINER واحدة واحدة.
- تفعيل Leaked Password Protection.
- إضافة الفهارس الثمانية بعد التحقق من الحاجة.
- تقليل `select('*')` والاستعلامات الشاملة.
- مراجعة Edge Functions والصلاحيات.
- تشغيل Security/Performance Advisors بعد التعديل.

### الحزمة G — الاختبار والنشر

- اختبارات E2E للأدوار الثلاثة.
- Visual Regression للصفحات الأساسية.
- Accessibility Audit.
- حزمة `dist` نظيفة.
- نشر تجريبي، اختبار، ثم إصدار إنتاج واحد.

## معايير اعتماد 10/10

- لا توجد صفحات تستخدم إصدار Shell قديمًا.
- لا توجد ألوان أو مقاسات عشوائية خارج Tokens إلا باستثناء موثق.
- لا توجد رحلة حفظ بلا Loading وSuccess وError.
- صفحات المهام الأربع متطابقة في الهيكل والمكونات.
- لا توجد صفحة موظف خاصة مبنية على UID ثابت.
- الاستعلامات الكبيرة تستخدم أعمدة محددة وPagination أو Aggregation.
- جميع RPC الحساسة مدققة ومقيدة.
- لا توجد أسرار في الواجهة أو حزمة النشر.
- نجاح الاختبارات الحالية واختبارات E2E الجديدة.
- نجاح الاختبار بحساب الموظف والمدير ومدير النظام.
- نجاح Desktop/Tablet/Mobile وKeyboard Accessibility.
- تشغيل النظام المنشور دون أخطاء Console مؤثرة.

## ما لا ينبغي فعله

- عدم إضافة طبقة CSS جديدة فوق الطبقات الحالية دون تنظيفها.
- عدم إعادة كتابة النظام بإطار جديد في نفس الإصدار.
- عدم حذف Firebase Compatibility دفعة واحدة.
- عدم حذف الفهارس المصنفة “غير مستخدمة” دون فترة قياس كافية.
- عدم تحويل كل SECURITY DEFINER إلى INVOKER آليًا؛ بعض العمليات تحتاج صلاحيات مضبوطة مع تحقق داخلي.
- عدم نشر التغييرات قبل اختبارات الأدوار الثلاثة.

## الخلاصة

لا يوجد مانع تقني للوصول إلى 10/10. المطلوب ليس إعادة بناء النظام من الصفر، بل **تنظيف الأساس، تفكيك الأجزاء المتضخمة، توحيد المكونات، تحسين استعلامات Supabase، وتوسيع الاختبارات من فحص النصوص إلى رحلات مستخدم حقيقية**. التنفيذ يمكن أن يصل للمستخدمين كتحديث واحد، مع تطوير داخلي مرحلي وآمن.
