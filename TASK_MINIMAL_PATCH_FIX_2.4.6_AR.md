# إصلاح بدء المهمة ونسبة الإنجاز 2.4.6

## السبب المؤكد

كانت الواجهة ترسل جميع حقول المهمة في كل تحديث، ومن بينها `manager_notes`، حتى عندما لم يغير الموظف ملاحظات المدير. إذا كانت القيمة في قاعدة البيانات فارغة بصيغة `NULL` كانت الواجهة تحولها إلى نص فارغ، فيتعامل معها النظام كتعديل من الموظف ويرفض العملية برسالة `employee cannot modify manager notes`.

## الإصلاح

- إرسال الحقول التي تغيّرت فقط إلى `update_task_safe`.
- استبعاد ملاحظات المدير من بدء المهمة وتغيير نسبة الإنجاز ما لم يغيّرها مستخدم مخول فعلًا.
- الإبقاء على إعادة المحاولة الآمنة عند تعارض الإصدار الحقيقي.
- تحديث أرقام ملفات JavaScript إلى `1.9.20` لتجاوز التخزين المؤقت.

## التطبيق

ارفع الملفات مع المحافظة على المسارات، وانتظر نجاح `ATWAR Quality Gate` و`pages-build-deployment`، ثم اختبر بدء المهمة وتغيير النسبة بحساب عباس بخش. لا يحتاج هذا الإصلاح إلى تنفيذ ملف SQL جديد.
