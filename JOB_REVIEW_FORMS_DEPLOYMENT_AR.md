# تحديث مراجعة الوصف الوظيفي ومكتبة النماذج

## ترتيب الرفع

1. نفّذ الملف التالي كاملًا في Supabase SQL Editor:
   `supabase/migrations/20260914_job_review_proposals_and_forms.sql`
2. بعد نجاح SQL ارفع ملفات الحزمة إلى GitHub بالمسارات نفسها.
3. انتظر نجاح **ATWAR Quality Gate** و **pages-build-deployment**.
4. نفّذ تحديثًا قويًا للمتصفح باستخدام `Ctrl + F5`.

## مسار المراجعة الجديد

- مدير النظام يرسل الوصف إلى المدير المباشر.
- المدير لا يغيّر النسخة الرئيسية؛ يستطيع لكل نقطة اقتراح تعديل أو حذف أو إضافة ملاحظة، كما يستطيع إضافة نقطة جديدة.
- مسؤول النظام يفتح **مقارنة المقترحات** ويقرر لكل اقتراح: قبول وتطبيق، تعديل ثم قبول، أو رفض.
- لا يمكن إنهاء المراجعة الإدارية قبل اتخاذ قرار في جميع المقترحات المعلقة.
- الموظف يستمر في رؤية آخر نسخة منشورة حتى اعتماد ونشر النسخة الجديدة.

## مكتبة النماذج

- افتح **إدارة النظام ← مكتبة النماذج والأدلة**.
- أضف النموذج مرة واحدة، ثم انشره واربطه بالوظائف المطلوبة.
- لا يظهر للموظف إلا النموذج المنشور والمرتبط بوظيفته.

## فحص بعد الرفع

- بحساب مدير: تأكد من عدم وجود تعديل مباشر ومن ظهور أزرار الاقتراح عند كل نقطة.
- بحساب مدير النظام: تأكد من ظهور المقارنة والقرارات الثلاثة.
- بحساب موظف: تأكد من ظهور النسخة المنشورة فقط وبطاقات المهام ومصفوفة الصلاحيات.
