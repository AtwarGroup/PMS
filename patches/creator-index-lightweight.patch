diff --git a/assets/js/supabase-firebase-compat.js b/assets/js/supabase-firebase-compat.js
--- a/assets/js/supabase-firebase-compat.js
+++ b/assets/js/supabase-firebase-compat.js
@@
-  if(parts[0]==='createdTaskIndex'&&parts[1]){const rows=await visibleTasks({creator:parts[1]}),out={};for(const t of rows)out[publicKey(t._relationalId)]=t.assignUid;return new Snap(out,parts[1])}
+  if(parts[0]==='createdTaskIndex'&&parts[1]){
+    // The creator index only needs task IDs and assignees. Never hydrate activity,
+    // subtasks, attachments or profiles for an index lookup.
+    const {data,error}=await sb.from('tasks').select('id,assignee_id').eq('creator_id',parts[1]).is('deleted_at',null);
+    if(error)throw error;
+    const out={};for(const t of data||[])out[publicKey(t.id)]=t.assignee_id;
+    return new Snap(out,parts[1]);
+  }
